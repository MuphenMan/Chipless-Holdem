#include <iostream>
#include <cstdlib>
#include <string>
#include "Player.h"
#include "Game.h"

using namespace std;

int main() {

	int numPlayers;
	char playerChoice;

	

	for (int i = 0, i < numPlayers; i++) {

	}

	Game g(p, numPlayers);

	while (playerChoice != 7) {



		if (playerChoice == 7) {
			cout << "Are you sure? (y/n): ";
			cin >> playerChoice;
			if (playerChoice == y) {
				displayTotals();
				exit();
			}
			else
				continue;
		}



	


}


}